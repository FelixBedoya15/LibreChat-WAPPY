diff --git a/node_modules/@librechat/agents/dist/cjs/events.cjs b/node_modules/@librechat/agents/dist/cjs/events.cjs
index 1ebb7f6..acbea50 100644
--- a/node_modules/@librechat/agents/dist/cjs/events.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/events.cjs
@@ -29,6 +29,14 @@ class ModelEndHandler {
         if (usage != null && this.collectedUsage != null) {
             this.collectedUsage.push(usage);
         }
+        if (data?.output && data?.output?.tool_calls && data.output.additional_kwargs && data.output.additional_kwargs.__gemini_function_call_thought_signatures__) {
+            const sigs = data.output.additional_kwargs.__gemini_function_call_thought_signatures__;
+            data.output.tool_calls.forEach((tc, index) => {
+                if (sigs[index]) {
+                    tc.signature = sigs[index];
+                }
+            });
+        }
         if (metadata.ls_provider === 'FakeListChatModel') {
             return handlers.handleToolCalls(data?.output?.tool_calls, metadata, graph);
         }
diff --git a/node_modules/@librechat/agents/dist/cjs/graphs/MultiAgentGraph.cjs b/node_modules/@librechat/agents/dist/cjs/graphs/MultiAgentGraph.cjs
index 702a29d..4e48e88 100644
--- a/node_modules/@librechat/agents/dist/cjs/graphs/MultiAgentGraph.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/graphs/MultiAgentGraph.cjs
@@ -163,7 +163,7 @@ class MultiAgentGraph extends Graph.StandardGraph {
                 });
                 return new langgraph.Command({
                     goto: destination,
-                    update: { messages: state.messages.concat(toolMessage) },
+                    update: { messages: [toolMessage] },
                     graph: langgraph.Command.PARENT,
                 });
             }, {
@@ -207,7 +207,7 @@ class MultiAgentGraph extends Graph.StandardGraph {
                     const state = langgraph.getCurrentTaskInput();
                     return new langgraph.Command({
                         goto: destination,
-                        update: { messages: state.messages.concat(toolMessage) },
+                        update: { messages: [toolMessage] },
                         graph: langgraph.Command.PARENT,
                     });
                 }, {
diff --git a/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs b/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs
index 48c6de6..7568bdc 100644
--- a/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs
@@ -72,6 +72,14 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
         });
         this.streamUsage = fields.streamUsage ?? this.streamUsage;
     }
+
+    get _isMultimodalModel() {
+        return (this.model.includes('vision') ||
+            this.model.startsWith('gemini-1.5') ||
+            this.model.startsWith('gemini-2') ||
+            this.model.startsWith('gemini-3'));
+    }
+
     static lc_name() {
         return 'LibreChatGoogleGenerativeAI';
     }
@@ -102,9 +110,79 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
             ...parameters,
             contents: actualPrompt,
         };
+        if (Array.isArray(request.contents)) {
+            for (const item of request.contents) {
+                if (item && Array.isArray(item.parts)) {
+                    item.parts = item.parts.filter((part) => {
+                        if (part && part.inlineData && typeof part.inlineData.data === 'string' && part.inlineData.data.length > 35000000) {
+                            return false;
+                        }
+                        if (part && part.inline_data && typeof part.inline_data.data === 'string' && part.inline_data.data.length > 35000000) {
+                            return false;
+                        }
+                        return true;
+                    });
+                }
+            }
+        }
+        if (this.client?.systemInstruction && Array.isArray(this.client.systemInstruction.parts)) {
+            this.client.systemInstruction.parts = this.client.systemInstruction.parts.map((part) => {
+                if (part && typeof part.text === 'string' && part.text.length > 1000000) {
+                    return { text: part.text.substring(0, 500000) + '\n\n[...Instrucciones truncadas por límite de tamaño...]' };
+                }
+                return part;
+            });
+        }
+        if (Array.isArray(request.tools)) {
+            const sanitizeSchema = (schema) => {
+                if (!schema || typeof schema !== 'object') return;
+                if (Array.isArray(schema.type)) {
+                    schema.type = schema.type[0] || 'string';
+                }
+                if (schema.properties && typeof schema.properties === 'object') {
+                    for (const prop of Object.values(schema.properties)) {
+                        sanitizeSchema(prop);
+                    }
+                }
+                if (schema.items && typeof schema.items === 'object') {
+                    sanitizeSchema(schema.items);
+                }
+            };
+            for (const tool of request.tools) {
+                if (Array.isArray(tool.functionDeclarations)) {
+                    for (const fn of tool.functionDeclarations) {
+                        if (fn && fn.parameters) {
+                            sanitizeSchema(fn.parameters);
+                        }
+                    }
+                }
+            }
+        }
         const stream = await this.caller.callWithOptions({ signal: options.signal }, async () => {
+            const clientToUse = new generativeAi.GoogleGenerativeAI(this.apiKey).getGenerativeModel({
+                model: this.model,
+                safetySettings: this.safetySettings,
+                generationConfig: {
+                    stopSequences: this.stopSequences,
+                    maxOutputTokens: this.maxOutputTokens,
+                    temperature: this.temperature,
+                    topP: this.topP,
+                    topK: this.topK,
+                    ...(this.json != null
+                        ? { responseMimeType: 'application/json' }
+                        : {}),
+                    ...(this.thinkingConfig != null
+                        ? { thinkingConfig: this.thinkingConfig }
+                        : {}),
+                },
+                systemInstruction: this.client?.systemInstruction,
+            }, {
+                apiVersion: this.apiVersion,
+                baseUrl: this.baseUrl,
+                customHeaders: this.customHeaders,
+            });
             /** @ts-ignore */
-            const { stream } = await this.client.generateContentStream(request);
+            const { stream } = await clientToUse.generateContentStream(request);
             return stream;
         });
         let lastUsageMetadata;
diff --git a/node_modules/@librechat/agents/dist/cjs/llm/google/utils/common.cjs b/node_modules/@librechat/agents/dist/cjs/llm/google/utils/common.cjs
index 450e0d6..1c7cf21 100644
--- a/node_modules/@librechat/agents/dist/cjs/llm/google/utils/common.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/llm/google/utils/common.cjs
@@ -329,13 +329,21 @@ function convertMessageContentToParts(message, isMultimodalModel, previousMessag
             .filter((p) => p !== undefined));
     }
     if (messages.isAIMessage(message) && message.tool_calls?.length != null) {
-        functionCalls = message.tool_calls.map((tc) => {
-            return {
-                functionCall: {
-                    name: tc.name,
-                    args: tc.args,
-                },
+        const sigs = message.additional_kwargs?.__gemini_function_call_thought_signatures__;
+        functionCalls = message.tool_calls.map((tc, idx) => {
+            const resultFunc = {
+                name: tc.name,
+                args: tc.args,
+            };
+            const part = {
+                functionCall: resultFunc,
             };
+            if (sigs && sigs[idx]) {
+                part.thought_signature = sigs[idx];
+            } else if (tc.signature) {
+                part.thought_signature = tc.signature;
+            }
+            return part;
         });
     }
     return [...messageParts, ...functionCalls];
@@ -350,13 +358,24 @@ function convertBaseMessagesToContent(messages$1, isMultimodalModel, convertSyst
             throw new Error('System message should be the first one');
         }
         const role = convertAuthorToRole(author);
-        const prevContent = acc.content?.[acc.content.length];
-        if (!acc.mergeWithPreviousContent &&
-            prevContent &&
-            prevContent.role === role) {
-            throw new Error('Google Generative AI requires alternate messages between authors');
+        
+        let actualRole = role;
+        if (actualRole === 'function' ||
+            (actualRole === 'system' && !convertSystemMessageToHumanContent)) {
+            actualRole = 'user';
         }
+
         const parts = convertMessageContentToParts(message, isMultimodalModel, messages$1.slice(0, index));
+
+        const prevContent = acc.content?.[acc.content.length - 1];
+        if (prevContent && prevContent.role === actualRole) {
+            prevContent.parts.push(...parts);
+            return {
+                mergeWithPreviousContent: acc.mergeWithPreviousContent,
+                content: acc.content,
+            };
+        }
+
         if (acc.mergeWithPreviousContent) {
             const prevContent = acc.content?.[acc.content.length - 1];
             if (!prevContent) {
@@ -368,12 +387,7 @@ function convertBaseMessagesToContent(messages$1, isMultimodalModel, convertSyst
                 content: acc.content,
             };
         }
-        let actualRole = role;
-        if (actualRole === 'function' ||
-            (actualRole === 'system' && !convertSystemMessageToHumanContent)) {
-            // GenerativeAI API will throw an error if the role is not "user" or "model."
-            actualRole = 'user';
-        }
+
         const content = {
             role: actualRole,
             parts,
@@ -409,30 +423,35 @@ function convertResponseContentToChatGenerationChunk(response, extra) {
         content = textParts.join('');
     }
     else if (candidateContent && Array.isArray(candidateContent.parts)) {
-        content = candidateContent.parts.map((p) => {
+        const nonThoughtParts = [];
+        for (const p of candidateContent.parts) {
             if ('text' in p && 'thought' in p && p.thought === true) {
                 reasoningParts.push(p.text ?? '');
+                continue;
             }
-            else if ('text' in p) {
-                return {
+            if ('text' in p) {
+                nonThoughtParts.push({
                     type: 'text',
                     text: p.text,
-                };
+                });
             }
             else if ('executableCode' in p) {
-                return {
+                nonThoughtParts.push({
                     type: 'executableCode',
                     executableCode: p.executableCode,
-                };
+                });
             }
             else if ('codeExecutionResult' in p) {
-                return {
+                nonThoughtParts.push({
                     type: 'codeExecutionResult',
                     codeExecutionResult: p.codeExecutionResult,
-                };
+                });
             }
-            return p;
-        });
+            else if (p && typeof p === 'object') {
+                nonThoughtParts.push(p);
+            }
+        }
+        content = nonThoughtParts;
     }
     else {
         // no content returned - likely due to abnormal stop reason, e.g. malformed function call
@@ -443,7 +462,7 @@ function convertResponseContentToChatGenerationChunk(response, extra) {
         text = content;
     }
     else if (Array.isArray(content)) {
-        const block = content.find((b) => 'text' in b);
+        const block = content.find((b) => b && typeof b === 'object' && 'text' in b);
         text = block?.text ?? '';
     }
     const toolCallChunks = [];
@@ -458,6 +477,20 @@ function convertResponseContentToChatGenerationChunk(response, extra) {
         })));
     }
     const additional_kwargs = {};
+    const signatures = [];
+    if (candidateContent && Array.isArray(candidateContent.parts)) {
+        for (const pt of candidateContent.parts) {
+            if (pt?.functionCall) {
+                if (pt.thoughtSignature) signatures.push(pt.thoughtSignature);
+                else if (pt.thought_signature) signatures.push(pt.thought_signature);
+                else if (pt.functionCall.thought_signature) signatures.push(pt.functionCall.thought_signature);
+                else if (pt.functionCall.thoughtSignature) signatures.push(pt.functionCall.thoughtSignature);
+            }
+        }
+    }
+    if (signatures.length > 0) {
+        additional_kwargs.__gemini_function_call_thought_signatures__ = signatures;
+    }
     if (reasoningParts.length > 0) {
         additional_kwargs.reasoning = reasoningParts.join('');
     }
diff --git a/node_modules/@librechat/agents/dist/cjs/messages/format.cjs b/node_modules/@librechat/agents/dist/cjs/messages/format.cjs
index 3579d89..a3e4b1e 100644
--- a/node_modules/@librechat/agents/dist/cjs/messages/format.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/messages/format.cjs
@@ -78,7 +78,11 @@ const formatMessage = ({ message, userName, endpoint, assistantName, langChain =
     const { image_urls, documents, videos, audios } = message;
     const mediaParts = [];
     if (Array.isArray(documents) && documents.length > 0) {
-        mediaParts.push(...documents);
+        const safeDocs = documents.filter((doc) => {
+            const len = doc?.data?.length || doc?.file_data?.length || 0;
+            return len <= 35000000;
+        });
+        mediaParts.push(...safeDocs);
     }
     if (Array.isArray(videos) && videos.length > 0) {
         mediaParts.push(...videos);
@@ -87,7 +91,11 @@ const formatMessage = ({ message, userName, endpoint, assistantName, langChain =
         mediaParts.push(...audios);
     }
     if (Array.isArray(image_urls) && image_urls.length > 0) {
-        mediaParts.push(...image_urls);
+        const safeImages = image_urls.filter((img) => {
+            const urlStr = typeof img === 'string' ? img : (img?.image_url?.url || img?.url || '');
+            return urlStr.length <= 35000000;
+        });
+        mediaParts.push(...safeImages);
     }
     if (mediaParts.length > 0 && role === 'user') {
         const mediaMessage = formatMediaMessage({
@@ -212,6 +220,13 @@ function formatAssistantMessage(message) {
                     lastAIMessage.tool_calls = [];
                 }
                 lastAIMessage.tool_calls.push(tool_call);
+                if (tool_call.signature) {
+                    if (!lastAIMessage.additional_kwargs) lastAIMessage.additional_kwargs = {};
+                    if (!lastAIMessage.additional_kwargs.__gemini_function_call_thought_signatures__) {
+                        lastAIMessage.additional_kwargs.__gemini_function_call_thought_signatures__ = [];
+                    }
+                    lastAIMessage.additional_kwargs.__gemini_function_call_thought_signatures__.push(tool_call.signature);
+                }
                 formattedMessages.push(new messages.ToolMessage({
                     tool_call_id: tool_call.id ?? '',
                     name: tool_call.name,
diff --git a/node_modules/@librechat/agents/dist/cjs/tools/search/firecrawl.cjs b/node_modules/@librechat/agents/dist/cjs/tools/search/firecrawl.cjs
index ad3dd64..96a790b 100644
--- a/node_modules/@librechat/agents/dist/cjs/tools/search/firecrawl.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/tools/search/firecrawl.cjs
@@ -35,6 +35,7 @@ class FirecrawlScraper {
         this.version = config.version ?? 'v2';
         const baseUrl = config.apiUrl ??
             process.env.FIRECRAWL_BASE_URL ??
+            process.env.FIRECRAWL_API_URL ??
             'https://api.firecrawl.dev';
         this.apiUrl = `${baseUrl.replace(/\/+$/, '')}/${this.version}/scrape`;
         this.defaultFormats = config.formats ?? ['markdown', 'rawHtml'];
diff --git a/node_modules/@librechat/agents/dist/cjs/tools/search/rerankers.cjs b/node_modules/@librechat/agents/dist/cjs/tools/search/rerankers.cjs
index 54e1e16..8eaa394 100644
--- a/node_modules/@librechat/agents/dist/cjs/tools/search/rerankers.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/tools/search/rerankers.cjs
@@ -13,7 +13,7 @@ class BaseReranker {
     getDefaultRanking(documents, topK) {
         return documents
             .slice(0, Math.min(topK, documents.length))
-            .map((doc) => ({ text: doc, score: 0 }));
+            .map((doc, index) => ({ text: doc, score: 0, index }));
     }
 }
 class JinaReranker extends BaseReranker {
@@ -30,51 +30,69 @@ class JinaReranker extends BaseReranker {
                 this.logger.warn('JINA_API_KEY is not set. Using default ranking.');
                 return this.getDefaultRanking(documents, topK);
             }
-            const requestData = {
-                model: 'jina-reranker-v2-base-multilingual',
-                query: query,
-                top_n: topK,
-                documents: documents,
-                return_documents: true,
-            };
-            const response = await axios.post(this.apiUrl, requestData, {
-                headers: {
-                    'Content-Type': 'application/json',
-                    Authorization: `Bearer ${this.apiKey}`,
-                },
-            });
-            this.logger.debug('Jina API Model:', response.data?.model);
-            this.logger.debug('Jina API Usage:', response.data?.usage);
-            if (response.data && response.data.results.length) {
+            const isTEI = !this.apiUrl.includes('jina.ai');
+            let targetUrl = this.apiUrl;
+            if (targetUrl.includes('://reranker')) {
+                targetUrl = targetUrl.replace('://reranker', '://librechat-reranker-d58plj');
+            }
+            if (isTEI && targetUrl.endsWith('/v1/rerank')) {
+                targetUrl = targetUrl.replace(/\/v1\/rerank$/, '/rerank');
+            }
+            const headers = { 'Content-Type': 'application/json' };
+            if (this.apiKey && this.apiKey !== 'local-dummy-key') {
+                headers['Authorization'] = 'Bearer ' + this.apiKey;
+            }
+            let requestData;
+            if (isTEI) {
+                requestData = {
+                    query: query,
+                    texts: documents,
+                    raw_scores: false,
+                    return_text: false,
+                };
+            } else {
+                requestData = {
+                    model: 'jina-reranker-v2-base-multilingual',
+                    query: query,
+                    top_n: topK,
+                    documents: documents,
+                    return_documents: true,
+                };
+            }
+            const response = await axios.post(targetUrl, requestData, { headers });
+            if (Array.isArray(response.data)) {
+                const sorted = [...response.data]
+                    .sort((a, b) => (b.score ?? 0) - (a.score ?? 0))
+                    .slice(0, topK);
+                return sorted.map((res) => {
+                    const docIndex = res.index;
+                    const score = res.score ?? 0;
+                    const text = documents[docIndex] ?? '';
+                    return { text, score, index: docIndex };
+                });
+            } else if (response.data && response.data.results && response.data.results.length) {
                 return response.data.results.map((result) => {
                     const docIndex = result.index;
                     const score = result.relevance_score;
                     let text = '';
-                    // If return_documents is true, the document field will be present
                     if (result.document != null) {
                         const doc = result.document;
                         if (typeof doc === 'object' && 'text' in doc) {
                             text = doc.text;
-                        }
-                        else if (typeof doc === 'string') {
+                        } else if (typeof doc === 'string') {
                             text = doc;
                         }
-                    }
-                    else {
-                        // Otherwise, use the index to get the document
+                    } else {
                         text = documents[docIndex];
                     }
-                    return { text, score };
+                    return { text, score, index: docIndex };
                 });
-            }
-            else {
-                this.logger.warn('Unexpected response format from Jina API. Using default ranking.');
+            } else {
+                this.logger.warn('Unexpected response format from reranker. Using default ranking.');
                 return this.getDefaultRanking(documents, topK);
             }
-        }
-        catch (error) {
-            this.logger.error('Error using Jina reranker:', error);
-            // Fallback to default ranking on error
+        } catch (error) {
+            this.logger.error('Error using reranker:', error);
             return this.getDefaultRanking(documents, topK);
         }
     }
@@ -140,26 +158,14 @@ class InfinityReranker extends BaseReranker {
  * Creates the appropriate reranker based on type and configuration
  */
 const createReranker = (config) => {
-    const { rerankerType, jinaApiKey, jinaApiUrl, cohereApiKey, logger } = config;
-    // Create a default logger if none is provided
+    const { jinaApiKey, jinaApiUrl, logger } = config || {};
     const defaultLogger = logger || utils.createDefaultLogger();
-    switch (rerankerType.toLowerCase()) {
-        case 'jina':
-            return new JinaReranker({ apiKey: jinaApiKey, apiUrl: jinaApiUrl, logger: defaultLogger });
-        case 'cohere':
-            return new CohereReranker({
-                apiKey: cohereApiKey,
-                logger: defaultLogger,
-            });
-        case 'infinity':
-            return new InfinityReranker(defaultLogger);
-        case 'none':
-            defaultLogger.debug('Skipping reranking as reranker is set to "none"');
-            return undefined;
-        default:
-            defaultLogger.warn(`Unknown reranker type: ${rerankerType}. Defaulting to InfinityReranker.`);
-            return new JinaReranker({ apiKey: jinaApiKey, apiUrl: jinaApiUrl, logger: defaultLogger });
+    let effectiveUrl = jinaApiUrl || process.env.RERANKER_API_URL || 'http://librechat-reranker-d58plj:80/rerank';
+    if (effectiveUrl.includes('://reranker')) {
+        effectiveUrl = effectiveUrl.replace('://reranker', '://librechat-reranker-d58plj');
     }
+    const effectiveKey = jinaApiKey || process.env.JINA_API_KEY || 'local-dummy-key';
+    return new JinaReranker({ apiKey: effectiveKey, apiUrl: effectiveUrl, logger: defaultLogger });
 };
 // Example usage:
 // const jinaReranker = new JinaReranker();
diff --git a/node_modules/@librechat/agents/dist/cjs/tools/search/schema.cjs b/node_modules/@librechat/agents/dist/cjs/tools/search/schema.cjs
index b5dfd0e..94c3cf8 100644
--- a/node_modules/@librechat/agents/dist/cjs/tools/search/schema.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/tools/search/schema.cjs
@@ -10,25 +10,8 @@ exports.DATE_RANGE = void 0;
     DATE_RANGE["PAST_MONTH"] = "m";
     DATE_RANGE["PAST_YEAR"] = "y";
 })(exports.DATE_RANGE || (exports.DATE_RANGE = {}));
-const DEFAULT_QUERY_DESCRIPTION = `
-GUIDELINES:
-- Start broad, then narrow: Begin with key concepts, then refine with specifics
-- Think like sources: Use terminology experts would use in the field
-- Consider perspective: Frame queries from different viewpoints for better results
-- Quality over quantity: A precise 3-4 word query often beats lengthy sentences
+const DEFAULT_QUERY_DESCRIPTION = 'The search query to look up on the web. Make it concise and relevant to the user request. Do not search for single generic words like "que" or "what".';
 
-TECHNIQUES (combine for power searches):
-- EXACT PHRASES: Use quotes ("climate change report")
-- EXCLUDE TERMS: Use minus to remove unwanted results (-wikipedia)
-- SITE-SPECIFIC: Restrict to websites (site:edu research)
-- FILETYPE: Find specific documents (filetype:pdf study)
-- OR OPERATOR: Find alternatives (electric OR hybrid cars)
-- DATE RANGE: Recent information (data after:2020)
-- WILDCARDS: Use * for unknown terms (how to * bread)
-- SPECIFIC QUESTIONS: Use who/what/when/where/why/how
-- DOMAIN TERMS: Include technical terminology for specialized topics
-- CONCISE TERMS: Prioritize keywords over sentences
-`.trim();
 const DEFAULT_COUNTRY_DESCRIPTION = `Country code to localize search results.
 Use standard 2-letter country codes: "us", "uk", "ca", "de", "fr", "jp", "br", etc.
 Provide this when the search should return results specific to a particular country.
diff --git a/node_modules/@librechat/agents/dist/cjs/tools/search/search.cjs b/node_modules/@librechat/agents/dist/cjs/tools/search/search.cjs
index 7f57687..1eb7162 100644
--- a/node_modules/@librechat/agents/dist/cjs/tools/search/search.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/tools/search/search.cjs
@@ -184,9 +184,9 @@ const createSearXNGAPI = (instanceUrl, apiKey) => {
                 format: 'json',
                 pageno: 1,
                 categories: category,
-                language: 'all',
+                language: process.env.SEARXNG_LANGUAGE || 'es-CO',
                 safesearch: safeSearch,
-                engines: 'google,bing,duckduckgo',
+                engines: process.env.SEARXNG_ENGINES || 'yandex,wikipedia',
             };
             const headers = {
                 'Content-Type': 'application/json',
@@ -225,7 +225,18 @@ const createSearXNGAPI = (instanceUrl, apiKey) => {
                 return hasNewsKeywords || hasNewsPath;
             };
             // Transform SearXNG results to match SerperAPI format
-            const organicResults = (data.results ?? [])
+            const blockedDomains = ['corporatefinanceinstitute.com', 'gao.gov', 'doordash.com', 'clipchamp.com', 'intervalworld.com', 'aol.com', 'microsoft.com', 'doubleclick.net', 'googleadservices.com', 'vineyardvines.com', 'zhihu.com', 'arbetsformedlingen.se', 'ledigajobb.se', 'healthgrades.com', 'vitadox.com', 'orthopedic.io', 'meudanfe.com.br', 'softonic.com', 'droidcam.com', 'capterra.com', 'g2.com', 'trustpilot.com', 'pinterest.com', 'anu.edu.au', '.edu.au', 'reddit.com', 'redd.it', 'everfulwholesale.com', 'towelsupercenter.com', 'xvideos.com', 'xvideos'];
+            const rawResults = (data.results ?? []).filter((r) => {
+                if (!r.url || !r.title) return false;
+                if (r.url.toLowerCase().includes('.pdf')) return false;
+                try {
+                    const host = new URL(r.url).hostname.toLowerCase();
+                    return !blockedDomains.some((d) => host.includes(d));
+                } catch {
+                    return false;
+                }
+            });
+            const organicResults = rawResults
                 .slice(0, numResults)
                 .map((result, index) => {
                 let attribution = '';
@@ -406,6 +417,9 @@ const createSourceProcessor = (config = {}, scraperInstance) => {
         },
     };
     const fetchContents = async ({ links, query, target, onGetHighlights, onContentScraped, }) => {
+        if (process.env.ENABLE_DEEP_SCRAPING !== 'true') {
+            return;
+        }
         const initialLinks = links.slice(0, target);
         // const remainingLinks = links.slice(target).reverse();
         const results = await webScraper.scrapeMany({
@@ -465,6 +479,33 @@ const createSourceProcessor = (config = {}, scraperInstance) => {
                     }
                 }
                 return result.data;
+            }
+                        if (reranker && result.data.organic && result.data.organic.length > 1) {
+                try {
+                    const searchSnippets = result.data.organic.map((s) => (s.title || '') + '. ' + (s.snippet || ''));
+                    const ranked = await reranker.rerank(query, searchSnippets, result.data.organic.length);
+                    if (Array.isArray(ranked) && ranked.length > 0) {
+                        const reordered = [];
+                        const seenIndices = new Set();
+                        for (const r of ranked) {
+                            if (typeof r.index === 'number' && result.data.organic[r.index] && !seenIndices.has(r.index)) {
+                                seenIndices.add(r.index);
+                                reordered.push(result.data.organic[r.index]);
+                            }
+                        }
+                        for (let i = 0; i < result.data.organic.length; i++) {
+                            if (!seenIndices.has(i)) {
+                                reordered.push(result.data.organic[i]);
+                            }
+                        }
+                        const targetLimit = Math.min(reordered.length, numElements || 5);
+                        result.data.organic = reordered.slice(0, targetLimit);
+                    }
+                } catch (e) {
+                    logger_.warn('Error pre-ranking organic search results:', e);
+                }
+            } else if (result.data.organic && result.data.organic.length > (numElements || 5)) {
+                result.data.organic = result.data.organic.slice(0, numElements || 5);
             }
             const sourceMap = new Map();
             const organicLinksSet = new Set();
@@ -505,6 +546,9 @@ const createSourceProcessor = (config = {}, scraperInstance) => {
             if (news && topStories.length > 0) {
                 updateSourcesWithContent(topStories, sourceMap);
             }
+            if (result.data && Array.isArray(result.data.organic) && result.data.organic.length > (numElements || 5)) {
+                result.data.organic = result.data.organic.slice(0, numElements || 5);
+            }
             return result.data;
         }
         catch (error) {
diff --git a/node_modules/@librechat/agents/dist/cjs/tools/search/tool.cjs b/node_modules/@librechat/agents/dist/cjs/tools/search/tool.cjs
index 01c1078..c8dafed 100644
--- a/node_modules/@librechat/agents/dist/cjs/tools/search/tool.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/tools/search/tool.cjs
@@ -147,7 +147,6 @@ function createSearchProcessor({ searchAPI, safeSearch, sourceProcessor, onGetHi
                 news,
                 logger,
             });
-            onSearchResults?.(searchResult);
             const processedSources = await sourceProcessor.processSources({
                 query,
                 news,
@@ -156,6 +155,7 @@ function createSearchProcessor({ searchAPI, safeSearch, sourceProcessor, onGetHi
                 onGetHighlights,
                 numElements: maxSources,
             });
+            onSearchResults?.({ success: true, data: processedSources });
             return highlights.expandHighlights(processedSources);
         }
         catch (error) {
diff --git a/node_modules/@librechat/agents/dist/esm/graphs/MultiAgentGraph.mjs b/node_modules/@librechat/agents/dist/esm/graphs/MultiAgentGraph.mjs
index 2ce0a08..9bd5b62 100644
--- a/node_modules/@librechat/agents/dist/esm/graphs/MultiAgentGraph.mjs
+++ b/node_modules/@librechat/agents/dist/esm/graphs/MultiAgentGraph.mjs
@@ -161,7 +161,7 @@ class MultiAgentGraph extends StandardGraph {
                 });
                 return new Command({
                     goto: destination,
-                    update: { messages: state.messages.concat(toolMessage) },
+                    update: { messages: [toolMessage] },
                     graph: Command.PARENT,
                 });
             }, {
@@ -205,7 +205,7 @@ class MultiAgentGraph extends StandardGraph {
                     const state = getCurrentTaskInput();
                     return new Command({
                         goto: destination,
-                        update: { messages: state.messages.concat(toolMessage) },
+                        update: { messages: [toolMessage] },
                         graph: Command.PARENT,
                     });
                 }, {
diff --git a/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs b/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs
index f936ded..9c2e380 100644
--- a/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs
+++ b/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs
@@ -70,6 +70,14 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
         });
         this.streamUsage = fields.streamUsage ?? this.streamUsage;
     }
+
+    get _isMultimodalModel() {
+        return (this.model.includes('vision') ||
+            this.model.startsWith('gemini-1.5') ||
+            this.model.startsWith('gemini-2') ||
+            this.model.startsWith('gemini-3'));
+    }
+
     static lc_name() {
         return 'LibreChatGoogleGenerativeAI';
     }
@@ -100,6 +108,31 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
             ...parameters,
             contents: actualPrompt,
         };
+        if (Array.isArray(request.tools)) {
+            const sanitizeSchema = (schema) => {
+                if (!schema || typeof schema !== 'object') return;
+                if (Array.isArray(schema.type)) {
+                    schema.type = schema.type[0] || 'string';
+                }
+                if (schema.properties && typeof schema.properties === 'object') {
+                    for (const prop of Object.values(schema.properties)) {
+                        sanitizeSchema(prop);
+                    }
+                }
+                if (schema.items && typeof schema.items === 'object') {
+                    sanitizeSchema(schema.items);
+                }
+            };
+            for (const tool of request.tools) {
+                if (Array.isArray(tool.functionDeclarations)) {
+                    for (const fn of tool.functionDeclarations) {
+                        if (fn && fn.parameters) {
+                            sanitizeSchema(fn.parameters);
+                        }
+                    }
+                }
+            }
+        }
         const stream = await this.caller.callWithOptions({ signal: options.signal }, async () => {
             /** @ts-ignore */
             const { stream } = await this.client.generateContentStream(request);
diff --git a/node_modules/@librechat/agents/dist/esm/llm/google/utils/common.mjs b/node_modules/@librechat/agents/dist/esm/llm/google/utils/common.mjs
index 02305d8..e6673df 100644
--- a/node_modules/@librechat/agents/dist/esm/llm/google/utils/common.mjs
+++ b/node_modules/@librechat/agents/dist/esm/llm/google/utils/common.mjs
@@ -348,13 +348,24 @@ function convertBaseMessagesToContent(messages, isMultimodalModel, convertSystem
             throw new Error('System message should be the first one');
         }
         const role = convertAuthorToRole(author);
-        const prevContent = acc.content?.[acc.content.length];
-        if (!acc.mergeWithPreviousContent &&
-            prevContent &&
-            prevContent.role === role) {
-            throw new Error('Google Generative AI requires alternate messages between authors');
+        
+        let actualRole = role;
+        if (actualRole === 'function' ||
+            (actualRole === 'system' && !convertSystemMessageToHumanContent)) {
+            actualRole = 'user';
         }
+
         const parts = convertMessageContentToParts(message, isMultimodalModel, messages.slice(0, index));
+
+        const prevContent = acc.content?.[acc.content.length - 1];
+        if (prevContent && prevContent.role === actualRole) {
+            prevContent.parts.push(...parts);
+            return {
+                mergeWithPreviousContent: acc.mergeWithPreviousContent,
+                content: acc.content,
+            };
+        }
+
         if (acc.mergeWithPreviousContent) {
             const prevContent = acc.content?.[acc.content.length - 1];
             if (!prevContent) {
@@ -366,12 +377,7 @@ function convertBaseMessagesToContent(messages, isMultimodalModel, convertSystem
                 content: acc.content,
             };
         }
-        let actualRole = role;
-        if (actualRole === 'function' ||
-            (actualRole === 'system' && !convertSystemMessageToHumanContent)) {
-            // GenerativeAI API will throw an error if the role is not "user" or "model."
-            actualRole = 'user';
-        }
+
         const content = {
             role: actualRole,
             parts,
@@ -407,30 +413,35 @@ function convertResponseContentToChatGenerationChunk(response, extra) {
         content = textParts.join('');
     }
     else if (candidateContent && Array.isArray(candidateContent.parts)) {
-        content = candidateContent.parts.map((p) => {
+        const nonThoughtParts = [];
+        for (const p of candidateContent.parts) {
             if ('text' in p && 'thought' in p && p.thought === true) {
                 reasoningParts.push(p.text ?? '');
+                continue;
             }
-            else if ('text' in p) {
-                return {
+            if ('text' in p) {
+                nonThoughtParts.push({
                     type: 'text',
                     text: p.text,
-                };
+                });
             }
             else if ('executableCode' in p) {
-                return {
+                nonThoughtParts.push({
                     type: 'executableCode',
                     executableCode: p.executableCode,
-                };
+                });
             }
             else if ('codeExecutionResult' in p) {
-                return {
+                nonThoughtParts.push({
                     type: 'codeExecutionResult',
                     codeExecutionResult: p.codeExecutionResult,
-                };
+                });
             }
-            return p;
-        });
+            else if (p && typeof p === 'object') {
+                nonThoughtParts.push(p);
+            }
+        }
+        content = nonThoughtParts;
     }
     else {
         // no content returned - likely due to abnormal stop reason, e.g. malformed function call
@@ -441,7 +452,7 @@ function convertResponseContentToChatGenerationChunk(response, extra) {
         text = content;
     }
     else if (Array.isArray(content)) {
-        const block = content.find((b) => 'text' in b);
+        const block = content.find((b) => b && typeof b === 'object' && 'text' in b);
         text = block?.text ?? '';
     }
     const toolCallChunks = [];
diff --git a/node_modules/@librechat/agents/dist/esm/tools/search/firecrawl.mjs b/node_modules/@librechat/agents/dist/esm/tools/search/firecrawl.mjs
index c75aa27..6eccf9b 100644
--- a/node_modules/@librechat/agents/dist/esm/tools/search/firecrawl.mjs
+++ b/node_modules/@librechat/agents/dist/esm/tools/search/firecrawl.mjs
@@ -33,6 +33,7 @@ class FirecrawlScraper {
         this.version = config.version ?? 'v2';
         const baseUrl = config.apiUrl ??
             process.env.FIRECRAWL_BASE_URL ??
+            process.env.FIRECRAWL_API_URL ??
             'https://api.firecrawl.dev';
         this.apiUrl = `${baseUrl.replace(/\/+$/, '')}/${this.version}/scrape`;
         this.defaultFormats = config.formats ?? ['markdown', 'rawHtml'];
diff --git a/node_modules/@librechat/agents/dist/esm/tools/search/schema.mjs b/node_modules/@librechat/agents/dist/esm/tools/search/schema.mjs
index bf47fd9..abf44d5 100644
--- a/node_modules/@librechat/agents/dist/esm/tools/search/schema.mjs
+++ b/node_modules/@librechat/agents/dist/esm/tools/search/schema.mjs
@@ -8,25 +8,8 @@ var DATE_RANGE;
     DATE_RANGE["PAST_MONTH"] = "m";
     DATE_RANGE["PAST_YEAR"] = "y";
 })(DATE_RANGE || (DATE_RANGE = {}));
-const DEFAULT_QUERY_DESCRIPTION = `
-GUIDELINES:
-- Start broad, then narrow: Begin with key concepts, then refine with specifics
-- Think like sources: Use terminology experts would use in the field
-- Consider perspective: Frame queries from different viewpoints for better results
-- Quality over quantity: A precise 3-4 word query often beats lengthy sentences
+const DEFAULT_QUERY_DESCRIPTION = 'The search query to look up on the web. Make it concise and relevant to the user request. Do not search for single generic words like "que" or "what".';
 
-TECHNIQUES (combine for power searches):
-- EXACT PHRASES: Use quotes ("climate change report")
-- EXCLUDE TERMS: Use minus to remove unwanted results (-wikipedia)
-- SITE-SPECIFIC: Restrict to websites (site:edu research)
-- FILETYPE: Find specific documents (filetype:pdf study)
-- OR OPERATOR: Find alternatives (electric OR hybrid cars)
-- DATE RANGE: Recent information (data after:2020)
-- WILDCARDS: Use * for unknown terms (how to * bread)
-- SPECIFIC QUESTIONS: Use who/what/when/where/why/how
-- DOMAIN TERMS: Include technical terminology for specialized topics
-- CONCISE TERMS: Prioritize keywords over sentences
-`.trim();
 const DEFAULT_COUNTRY_DESCRIPTION = `Country code to localize search results.
 Use standard 2-letter country codes: "us", "uk", "ca", "de", "fr", "jp", "br", etc.
 Provide this when the search should return results specific to a particular country.
diff --git a/node_modules/@librechat/agents/dist/esm/tools/search/search.mjs b/node_modules/@librechat/agents/dist/esm/tools/search/search.mjs
index 62b7ecf..19774b4 100644
--- a/node_modules/@librechat/agents/dist/esm/tools/search/search.mjs
+++ b/node_modules/@librechat/agents/dist/esm/tools/search/search.mjs
@@ -182,9 +182,9 @@ const createSearXNGAPI = (instanceUrl, apiKey) => {
                 format: 'json',
                 pageno: 1,
                 categories: category,
-                language: 'all',
+                language: process.env.SEARXNG_LANGUAGE || 'es-CO',
                 safesearch: safeSearch,
-                engines: 'google,bing,duckduckgo',
+                engines: process.env.SEARXNG_ENGINES || 'yandex,brave,gmx,pubmed',
             };
             const headers = {
                 'Content-Type': 'application/json',
@@ -223,7 +223,17 @@ const createSearXNGAPI = (instanceUrl, apiKey) => {
                 return hasNewsKeywords || hasNewsPath;
             };
             // Transform SearXNG results to match SerperAPI format
-            const organicResults = (data.results ?? [])
+            const blockedDomains = ['vineyardvines.com', 'doubleclick.net', 'googleadservices.com'];
+            const rawResults = (data.results ?? []).filter((r) => {
+                if (!r.url || !r.title) return false;
+                try {
+                    const host = new URL(r.url).hostname.toLowerCase();
+                    return !blockedDomains.some((d) => host.includes(d));
+                } catch {
+                    return false;
+                }
+            });
+            const organicResults = rawResults
                 .slice(0, numResults)
                 .map((result, index) => {
                 let attribution = '';
diff --git a/node_modules/@librechat/agents/src/graphs/MultiAgentGraph.ts b/node_modules/@librechat/agents/src/graphs/MultiAgentGraph.ts
index 8bcddd9..b2a3452 100644
--- a/node_modules/@librechat/agents/src/graphs/MultiAgentGraph.ts
+++ b/node_modules/@librechat/agents/src/graphs/MultiAgentGraph.ts
@@ -197,7 +197,7 @@ export class MultiAgentGraph extends StandardGraph {
 
             return new Command({
               goto: destination,
-              update: { messages: state.messages.concat(toolMessage) },
+              update: { messages: [toolMessage] },
               graph: Command.PARENT,
             });
           },
@@ -256,7 +256,7 @@ export class MultiAgentGraph extends StandardGraph {
 
               return new Command({
                 goto: destination,
-                update: { messages: state.messages.concat(toolMessage) },
+                update: { messages: [toolMessage] },
                 graph: Command.PARENT,
               });
             },
diff --git a/node_modules/@librechat/agents/src/llm/google/index.ts b/node_modules/@librechat/agents/src/llm/google/index.ts
index 2ab8c94..5368f2e 100644
--- a/node_modules/@librechat/agents/src/llm/google/index.ts
+++ b/node_modules/@librechat/agents/src/llm/google/index.ts
@@ -107,6 +107,15 @@ export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
     this.streamUsage = fields.streamUsage ?? this.streamUsage;
   }
 
+  get _isMultimodalModel(): boolean {
+    return (
+      this.model.includes('vision') ||
+      this.model.startsWith('gemini-1.5') ||
+      this.model.startsWith('gemini-2') ||
+      this.model.startsWith('gemini-3')
+    );
+  }
+
   static lc_name(): 'LibreChatGoogleGenerativeAI' {
     return 'LibreChatGoogleGenerativeAI';
   }
@@ -149,6 +158,31 @@ export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
       ...parameters,
       contents: actualPrompt,
     };
+    if (Array.isArray((request as any).tools)) {
+      const sanitizeSchema = (schema: any) => {
+        if (!schema || typeof schema !== 'object') return;
+        if (Array.isArray(schema.type)) {
+          schema.type = schema.type[0] || 'string';
+        }
+        if (schema.properties && typeof schema.properties === 'object') {
+          for (const prop of Object.values(schema.properties)) {
+            sanitizeSchema(prop);
+          }
+        }
+        if (schema.items && typeof schema.items === 'object') {
+          sanitizeSchema(schema.items);
+        }
+      };
+      for (const tool of (request as any).tools) {
+        if (Array.isArray(tool.functionDeclarations)) {
+          for (const fn of tool.functionDeclarations) {
+            if (fn && fn.parameters) {
+              sanitizeSchema(fn.parameters);
+            }
+          }
+        }
+      }
+    }
     const stream = await this.caller.callWithOptions(
       { signal: options.signal },
       async () => {
diff --git a/node_modules/@librechat/agents/src/llm/google/utils/common.ts b/node_modules/@librechat/agents/src/llm/google/utils/common.ts
index ccf070c..0e823ae 100644
--- a/node_modules/@librechat/agents/src/llm/google/utils/common.ts
+++ b/node_modules/@librechat/agents/src/llm/google/utils/common.ts
@@ -442,15 +442,12 @@ export function convertBaseMessagesToContent(
       }
       const role = convertAuthorToRole(author);
 
-      const prevContent = acc.content?.[acc.content.length];
+      let actualRole = role;
       if (
-        !acc.mergeWithPreviousContent &&
-        prevContent &&
-        prevContent.role === role
+        actualRole === 'function' ||
+        (actualRole === 'system' && !convertSystemMessageToHumanContent)
       ) {
-        throw new Error(
-          'Google Generative AI requires alternate messages between authors'
-        );
+        actualRole = 'user';
       }
 
       const parts = convertMessageContentToParts(
@@ -459,6 +456,15 @@ export function convertBaseMessagesToContent(
         messages.slice(0, index)
       );
 
+      const prevContent = acc.content?.[acc.content.length - 1];
+      if (prevContent && prevContent.role === actualRole) {
+        prevContent.parts.push(...parts);
+        return {
+          mergeWithPreviousContent: acc.mergeWithPreviousContent,
+          content: acc.content,
+        };
+      }
+
       if (acc.mergeWithPreviousContent) {
         const prevContent = acc.content?.[acc.content.length - 1];
         if (!prevContent) {
@@ -473,14 +479,7 @@ export function convertBaseMessagesToContent(
           content: acc.content,
         };
       }
-      let actualRole = role;
-      if (
-        actualRole === 'function' ||
-        (actualRole === 'system' && !convertSystemMessageToHumanContent)
-      ) {
-        // GenerativeAI API will throw an error if the role is not "user" or "model."
-        actualRole = 'user';
-      }
+
       const content: Content = {
         role: actualRole,
         parts,
diff --git a/node_modules/@librechat/agents/src/tools/search/schema.ts b/node_modules/@librechat/agents/src/tools/search/schema.ts
index 9a39d88..10ac23b 100644
--- a/node_modules/@librechat/agents/src/tools/search/schema.ts
+++ b/node_modules/@librechat/agents/src/tools/search/schema.ts
@@ -8,25 +8,8 @@ export enum DATE_RANGE {
   PAST_YEAR = 'y',
 }
 
-export const DEFAULT_QUERY_DESCRIPTION = `
-GUIDELINES:
-- Start broad, then narrow: Begin with key concepts, then refine with specifics
-- Think like sources: Use terminology experts would use in the field
-- Consider perspective: Frame queries from different viewpoints for better results
-- Quality over quantity: A precise 3-4 word query often beats lengthy sentences
+export const DEFAULT_QUERY_DESCRIPTION = 'The search query to look up on the web. Make it concise and relevant to the user request. Do not search for single generic words like "que" or "what".';
 
-TECHNIQUES (combine for power searches):
-- EXACT PHRASES: Use quotes ("climate change report")
-- EXCLUDE TERMS: Use minus to remove unwanted results (-wikipedia)
-- SITE-SPECIFIC: Restrict to websites (site:edu research)
-- FILETYPE: Find specific documents (filetype:pdf study)
-- OR OPERATOR: Find alternatives (electric OR hybrid cars)
-- DATE RANGE: Recent information (data after:2020)
-- WILDCARDS: Use * for unknown terms (how to * bread)
-- SPECIFIC QUESTIONS: Use who/what/when/where/why/how
-- DOMAIN TERMS: Include technical terminology for specialized topics
-- CONCISE TERMS: Prioritize keywords over sentences
-`.trim();
 
 export const DEFAULT_COUNTRY_DESCRIPTION =
   `Country code to localize search results.
diff --git a/node_modules/@librechat/agents/src/tools/search/search.ts b/node_modules/@librechat/agents/src/tools/search/search.ts
index e6b4aa8..121b53d 100644
--- a/node_modules/@librechat/agents/src/tools/search/search.ts
+++ b/node_modules/@librechat/agents/src/tools/search/search.ts
@@ -266,9 +266,9 @@ const createSearXNGAPI = (
         format: 'json',
         pageno: 1,
         categories: category,
-        language: 'all',
+        language: process.env.SEARXNG_LANGUAGE || 'es-CO',
         safesearch: safeSearch,
-        engines: 'google,bing,duckduckgo',
+        engines: (process.env.SEARXNG_ENGINES as string) || 'yandex,brave,gmx,pubmed',
       };
 
       const headers: Record<string, string> = {
