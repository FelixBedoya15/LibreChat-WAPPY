diff --git a/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs b/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs
index 48c6de6..1162a69 100644
--- a/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/llm/google/index.cjs
@@ -41,6 +41,12 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
                 'or in the `apiKey` field of the ' +
                 'ChatGoogleGenerativeAI constructor');
         }
+
+        // Parse potential comma-separated API keys
+        this._apiKeys = this.apiKey.split(',').map((k) => k.trim()).filter(Boolean);
+        this._currentKeyIndex = 0;
+        this.apiKey = this._apiKeys[this._currentKeyIndex] || this.apiKey;
+
         this.safetySettings = fields.safetySettings ?? this.safetySettings;
         if (this.safetySettings && this.safetySettings.length > 0) {
             const safetySettingsSet = new Set(this.safetySettings.map((s) => s.category));
@@ -51,6 +57,15 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
         this.thinkingConfig = fields.thinkingConfig ?? this.thinkingConfig;
         this.streaming = fields.streaming ?? this.streaming;
         this.json = fields.json;
+
+        // Save fields to easily re-initialize the client on rotation
+        this._modelFields = fields;
+
+        this._initializeClient();
+        this.streamUsage = fields.streamUsage ?? this.streamUsage;
+    }
+
+    _initializeClient() {
         // @ts-ignore - Accessing private property from parent class
         this.client = new generativeAi.GoogleGenerativeAI(this.apiKey).getGenerativeModel({
             model: this.model,
@@ -66,11 +81,23 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
                     : {}),
             },
         }, {
-            apiVersion: fields.apiVersion,
-            baseUrl: fields.baseUrl,
-            customHeaders: fields.customHeaders,
+            apiVersion: this._modelFields.apiVersion,
+            baseUrl: this._modelFields.baseUrl,
+            customHeaders: this._modelFields.customHeaders,
         });
-        this.streamUsage = fields.streamUsage ?? this.streamUsage;
+    }
+
+    rotateKey() {
+        if (this._apiKeys.length > 1) {
+            this._currentKeyIndex = (this._currentKeyIndex + 1) % this._apiKeys.length;
+            this.apiKey = this._apiKeys[this._currentKeyIndex];
+            this._initializeClient();
+            if (this._currentKeyIndex === 0) {
+                return false; // we wrapped around
+            }
+            return true; // Rotated to a new key
+        }
+        return false;
     }
     static lc_name() {
         return 'LibreChatGoogleGenerativeAI';
@@ -102,11 +129,36 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
             ...parameters,
             contents: actualPrompt,
         };
-        const stream = await this.caller.callWithOptions({ signal: options.signal }, async () => {
-            /** @ts-ignore */
-            const { stream } = await this.client.generateContentStream(request);
-            return stream;
-        });
+        let stream;
+        const attemptErrors = [];
+        while (true) {
+            try {
+                stream = await this.caller.callWithOptions({ signal: options.signal }, async () => {
+                    /** @ts-ignore */
+                    const { stream: genStream } = await this.client.generateContentStream(request);
+                    return genStream;
+                });
+                break;
+            } catch (e) {
+                const isQuotaEvent = e?.status === 429 || e?.message?.includes('429');
+                const isGenericQuota = e?.status === 403 || e?.message?.includes('403');
+                const isInvalidKey = e?.status === 400 || e?.message?.includes('API_KEY_INVALID');
+
+                attemptErrors.push(`[Key ${this._currentKeyIndex + 1}]: ` + (e?.message || 'Error'));
+
+                if ((isQuotaEvent || isGenericQuota || isInvalidKey) && this.rotateKey()) {
+                    console.warn(`[CustomChatGoogleGenerativeAI] Error (${isInvalidKey ? 'Invalid key' : 'Rate limit / Quota'}). Retrying with API key ${this._currentKeyIndex}...`);
+                    continue;
+                }
+
+                if (attemptErrors.length > 1) {
+                    const errorMsg = `All available API keys failed.\n` + attemptErrors.join('\n');
+                    throw new Error(errorMsg);
+                } else {
+                    throw e;
+                }
+            }
+        }
         let lastUsageMetadata;
         for await (const response of stream) {
             if ('usageMetadata' in response &&
@@ -122,7 +174,8 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
                 };
             }
             const chunk = common.convertResponseContentToChatGenerationChunk(response, {
-                usageMetadata: undefined});
+                usageMetadata: undefined
+            });
             if (!chunk) {
                 continue;
             }
@@ -141,6 +194,36 @@ class CustomChatGoogleGenerativeAI extends googleGenai.ChatGoogleGenerativeAI {
             await runManager?.handleLLMNewToken(finalChunk.text || '', undefined, undefined, undefined, undefined, { chunk: finalChunk });
         }
     }
+
+    async completionWithRetry(request, options) {
+        let res;
+        const attemptErrors = [];
+        while (true) {
+            try {
+                res = await super.completionWithRetry(request, options);
+                break;
+            } catch (e) {
+                const isQuotaEvent = e?.status === 429 || e?.message?.includes('429');
+                const isGenericQuota = e?.status === 403 || e?.message?.includes('403');
+                const isInvalidKey = e?.status === 400 || e?.message?.includes('API_KEY_INVALID');
+
+                attemptErrors.push(`[Key ${this._currentKeyIndex + 1}]: ` + (e?.message || 'Error'));
+
+                if ((isQuotaEvent || isGenericQuota || isInvalidKey) && this.rotateKey()) {
+                    console.warn(`[CustomChatGoogleGenerativeAI] completionWithRetry Error (${isInvalidKey ? 'Invalid key' : 'Rate limit / Quota'}). Retrying with API key ${this._currentKeyIndex}...`);
+                    continue;
+                }
+
+                if (attemptErrors.length > 1) {
+                    const errorMsg = `All available API keys failed.\n` + attemptErrors.join('\n');
+                    throw new Error(errorMsg);
+                } else {
+                    throw e;
+                }
+            }
+        }
+        return res;
+    }
 }
 
 exports.CustomChatGoogleGenerativeAI = CustomChatGoogleGenerativeAI;
diff --git a/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs b/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs
index f936ded..5555806 100644
--- a/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs
+++ b/node_modules/@librechat/agents/dist/esm/llm/google/index.mjs
@@ -39,6 +39,12 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
                 'or in the `apiKey` field of the ' +
                 'ChatGoogleGenerativeAI constructor');
         }
+
+        // Parse potential comma-separated API keys
+        this._apiKeys = this.apiKey.split(',').map((k) => k.trim()).filter(Boolean);
+        this._currentKeyIndex = 0;
+        this.apiKey = this._apiKeys[this._currentKeyIndex] || this.apiKey;
+
         this.safetySettings = fields.safetySettings ?? this.safetySettings;
         if (this.safetySettings && this.safetySettings.length > 0) {
             const safetySettingsSet = new Set(this.safetySettings.map((s) => s.category));
@@ -49,6 +55,15 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
         this.thinkingConfig = fields.thinkingConfig ?? this.thinkingConfig;
         this.streaming = fields.streaming ?? this.streaming;
         this.json = fields.json;
+
+        // Save fields to easily re-initialize the client on rotation
+        this._modelFields = fields;
+
+        this._initializeClient();
+        this.streamUsage = fields.streamUsage ?? this.streamUsage;
+    }
+
+    _initializeClient() {
         // @ts-ignore - Accessing private property from parent class
         this.client = new GoogleGenerativeAI(this.apiKey).getGenerativeModel({
             model: this.model,
@@ -64,11 +79,23 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
                     : {}),
             },
         }, {
-            apiVersion: fields.apiVersion,
-            baseUrl: fields.baseUrl,
-            customHeaders: fields.customHeaders,
+            apiVersion: this._modelFields.apiVersion,
+            baseUrl: this._modelFields.baseUrl,
+            customHeaders: this._modelFields.customHeaders,
         });
-        this.streamUsage = fields.streamUsage ?? this.streamUsage;
+    }
+
+    rotateKey() {
+        if (this._apiKeys.length > 1) {
+            this._currentKeyIndex = (this._currentKeyIndex + 1) % this._apiKeys.length;
+            this.apiKey = this._apiKeys[this._currentKeyIndex];
+            this._initializeClient();
+            if (this._currentKeyIndex === 0) {
+                return false; // we wrapped around
+            }
+            return true; // Rotated to a new key
+        }
+        return false;
     }
     static lc_name() {
         return 'LibreChatGoogleGenerativeAI';
@@ -100,11 +127,36 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
             ...parameters,
             contents: actualPrompt,
         };
-        const stream = await this.caller.callWithOptions({ signal: options.signal }, async () => {
-            /** @ts-ignore */
-            const { stream } = await this.client.generateContentStream(request);
-            return stream;
-        });
+        let stream;
+        const attemptErrors = [];
+        while (true) {
+            try {
+                stream = await this.caller.callWithOptions({ signal: options.signal }, async () => {
+                    /** @ts-ignore */
+                    const { stream: genStream } = await this.client.generateContentStream(request);
+                    return genStream;
+                });
+                break;
+            } catch (e) {
+                const isQuotaEvent = e?.status === 429 || e?.message?.includes('429');
+                const isGenericQuota = e?.status === 403 || e?.message?.includes('403');
+                const isInvalidKey = e?.status === 400 || e?.message?.includes('API_KEY_INVALID');
+
+                attemptErrors.push(`[Key ${this._currentKeyIndex + 1}]: ` + (e?.message || 'Error'));
+
+                if ((isQuotaEvent || isGenericQuota || isInvalidKey) && this.rotateKey()) {
+                    console.warn(`[CustomChatGoogleGenerativeAI] Error (${isInvalidKey ? 'Invalid key' : 'Rate limit / Quota'}). Retrying with API key ${this._currentKeyIndex}...`);
+                    continue;
+                }
+
+                if (attemptErrors.length > 1) {
+                    const errorMsg = `All available API keys failed.\n` + attemptErrors.join('\n');
+                    throw new Error(errorMsg);
+                } else {
+                    throw e;
+                }
+            }
+        }
         let lastUsageMetadata;
         for await (const response of stream) {
             if ('usageMetadata' in response &&
@@ -120,7 +172,8 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
                 };
             }
             const chunk = convertResponseContentToChatGenerationChunk(response, {
-                usageMetadata: undefined});
+                usageMetadata: undefined
+            });
             if (!chunk) {
                 continue;
             }
@@ -138,8 +191,36 @@ class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
             yield finalChunk;
             await runManager?.handleLLMNewToken(finalChunk.text || '', undefined, undefined, undefined, undefined, { chunk: finalChunk });
         }
+    async completionWithRetry(request, options) {
+            let res;
+            const attemptErrors = [];
+            while (true) {
+                try {
+                    res = await super.completionWithRetry(request, options);
+                    break;
+                } catch (e) {
+                    const isQuotaEvent = e?.status === 429 || e?.message?.includes('429');
+                    const isGenericQuota = e?.status === 403 || e?.message?.includes('403');
+                    const isInvalidKey = e?.status === 400 || e?.message?.includes('API_KEY_INVALID');
+
+                    attemptErrors.push(`[Key ${this._currentKeyIndex + 1}]: ` + (e?.message || 'Error'));
+
+                    if ((isQuotaEvent || isGenericQuota || isInvalidKey) && this.rotateKey()) {
+                        console.warn(`[CustomChatGoogleGenerativeAI] completionWithRetry Error (${isInvalidKey ? 'Invalid key' : 'Rate limit / Quota'}). Retrying with API key ${this._currentKeyIndex}...`);
+                        continue;
+                    }
+
+                    if (attemptErrors.length > 1) {
+                        const errorMsg = `All available API keys failed.\n` + attemptErrors.join('\n');
+                        throw new Error(errorMsg);
+                    } else {
+                        throw e;
+                    }
+                }
+            }
+            return res;
+        }
     }
-}
 
 export { CustomChatGoogleGenerativeAI };
 //# sourceMappingURL=index.mjs.map
diff --git a/node_modules/@librechat/agents/src/llm/google/index.ts b/node_modules/@librechat/agents/src/llm/google/index.ts
index 2ab8c94..a385bfe 100644
--- a/node_modules/@librechat/agents/src/llm/google/index.ts
+++ b/node_modules/@librechat/agents/src/llm/google/index.ts
@@ -20,6 +20,10 @@ import {
 
 export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
   thinkingConfig?: GeminiGenerationConfig['thinkingConfig'];
+  _apiKeys: string[];
+  _currentKeyIndex: number;
+  _modelFields: any;
+
   constructor(fields: GoogleClientOptions) {
     super(fields);
 
@@ -59,12 +63,17 @@ export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
     if (this.apiKey == null || this.apiKey === '') {
       throw new Error(
         'Please set an API key for Google GenerativeAI ' +
-          'in the environment variable GOOGLE_API_KEY ' +
-          'or in the `apiKey` field of the ' +
-          'ChatGoogleGenerativeAI constructor'
+        'in the environment variable GOOGLE_API_KEY ' +
+        'or in the `apiKey` field of the ' +
+        'ChatGoogleGenerativeAI constructor'
       );
     }
 
+    // Parse potential comma-separated API keys
+    this._apiKeys = this.apiKey.split(',').map((k) => k.trim()).filter(Boolean);
+    this._currentKeyIndex = 0;
+    this.apiKey = this._apiKeys[this._currentKeyIndex] || this.apiKey;
+
     this.safetySettings = fields.safetySettings ?? this.safetySettings;
     if (this.safetySettings && this.safetySettings.length > 0) {
       const safetySettingsSet = new Set(
@@ -82,6 +91,14 @@ export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
     this.streaming = fields.streaming ?? this.streaming;
     this.json = fields.json;
 
+    // Save fields to easily re-initialize the client on rotation
+    this._modelFields = fields;
+
+    this._initializeClient();
+    this.streamUsage = fields.streamUsage ?? this.streamUsage;
+  }
+
+  _initializeClient() {
     // @ts-ignore - Accessing private property from parent class
     this.client = new GenerativeAI(this.apiKey).getGenerativeModel(
       {
@@ -99,12 +116,24 @@ export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
         },
       },
       {
-        apiVersion: fields.apiVersion,
-        baseUrl: fields.baseUrl,
-        customHeaders: fields.customHeaders,
+        apiVersion: this._modelFields.apiVersion,
+        baseUrl: this._modelFields.baseUrl,
+        customHeaders: this._modelFields.customHeaders,
       }
     );
-    this.streamUsage = fields.streamUsage ?? this.streamUsage;
+  }
+
+  rotateKey(): boolean {
+    if (this._apiKeys.length > 1) {
+      this._currentKeyIndex = (this._currentKeyIndex + 1) % this._apiKeys.length;
+      this.apiKey = this._apiKeys[this._currentKeyIndex];
+      this._initializeClient();
+      if (this._currentKeyIndex === 0) {
+        return false; // we wrapped around
+      }
+      return true; // Rotated to a new key
+    }
+    return false;
   }
 
   static lc_name(): 'LibreChatGoogleGenerativeAI' {
@@ -149,14 +178,31 @@ export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
       ...parameters,
       contents: actualPrompt,
     };
-    const stream = await this.caller.callWithOptions(
-      { signal: options.signal },
-      async () => {
-        /** @ts-ignore */
-        const { stream } = await this.client.generateContentStream(request);
-        return stream;
+    let stream;
+    while (true) {
+      try {
+        stream = await this.caller.callWithOptions(
+          { signal: options.signal },
+          async () => {
+            /** @ts-ignore */
+            const { stream: genStream } = await this.client.generateContentStream(request);
+            return genStream;
+          }
+        );
+        break; // Successfully got the stream, escape retry loop
+      } catch (e: any) {
+        const isQuotaEvent = e?.status === 429 || e?.message?.includes('429');
+        const isGenericQuota = e?.status === 403 || e?.message?.includes('403');
+        const isInvalidKey = e?.status === 400 || e?.message?.includes('API_KEY_INVALID');
+
+        if ((isQuotaEvent || isGenericQuota || isInvalidKey) && this.rotateKey()) {
+          console.warn(`[CustomChatGoogleGenerativeAI] Error (${isInvalidKey ? 'Invalid key' : 'Rate limit / Quota'}). Retrying with API key ${this._currentKeyIndex}...`);
+          // Note: caller.callWithOptions respects its own retry logic as well, but this handles Google specific limits.
+          continue;
+        }
+        throw e;
       }
-    );
+    }
 
     let index = 0;
     let lastUsageMetadata: UsageMetadata | undefined;
@@ -219,4 +265,34 @@ export class CustomChatGoogleGenerativeAI extends ChatGoogleGenerativeAI {
       );
     }
   }
+
+  async completionWithRetry(request: any, options: any) {
+    let res;
+    const attemptErrors = [];
+    while (true) {
+      try {
+        res = await super.completionWithRetry(request, options);
+        break;
+      } catch (e: any) {
+        const isQuotaEvent = e?.status === 429 || e?.message?.includes('429');
+        const isGenericQuota = e?.status === 403 || e?.message?.includes('403');
+        const isInvalidKey = e?.status === 400 || e?.message?.includes('API_KEY_INVALID');
+
+        attemptErrors.push(`[Key ${this._currentKeyIndex + 1}]: ` + (e?.message || 'Error'));
+
+        if ((isQuotaEvent || isGenericQuota || isInvalidKey) && this.rotateKey()) {
+          console.warn(`[CustomChatGoogleGenerativeAI] completionWithRetry Error (${isInvalidKey ? 'Invalid key' : 'Rate limit / Quota'}). Retrying with API key ${this._currentKeyIndex}...`);
+          continue;
+        }
+
+        if (attemptErrors.length > 1) {
+          const errorMsg = `All available API keys failed.\n` + attemptErrors.join('\n');
+          throw new Error(errorMsg);
+        } else {
+          throw e;
+        }
+      }
+    }
+    return res;
+  }
 }
